
from setuptools import setup, find_packages

setup(
    name='timeseries_mc',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'numpy'
    ],
    author='Your Name',
    description='A Monte Carlo simulation package for time series data.',
)
